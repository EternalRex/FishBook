import 'package:flutter/material.dart';

class LoginLargeDesktopScreen extends StatelessWidget {
  const LoginLargeDesktopScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Hello Large Desktop'),
      ),
    );
  }
}
